# 本地开发环境变量 (wrangler pages dev)
# 复制本文件为 .dev.vars 并填入实际值

# 必填：目标服务器域名（不带 https://）
TARGET_DOMAIN = "my-api.vercel.app"

# 可选：认证 Token，设置并填写后启用 X-API-Key 请求头校验
# AUTH_TOKEN = "sk_live_abc123def456"

# 可选：需要剥离的路径前缀
# PATH_PREFIX = "/api"